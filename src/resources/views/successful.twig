{% extends 'base.twig' %}
{% block container %}
    <div class="container">
        <div class="row">
            <div class="col-12 text-center">
                <h1 class="display-1"><i class="fas fa-check-circle text-success"></i></h1>
            </div>
        </div>
        <div class="row mt-5">
            <div class="col-12">
                <h2 class="text-success text-center">Успешно сте завршили процес допуне.</h2>
            </div>
        </div>
        <div class="d-block d-sm-block d-md-block d-lg-none d-xl-none">
            <div class="row mt-5">
                <div class="col-12 text-center">
                    <a href="{{ path_for('invoice', {orderId: order_number}) }}" class="btn btn-info">
                        <i class="fas fa-file-pdf mr-2"></i> Преузмите потврду о плаћању
                    </a>
                </div>
            </div>
            <div class="row mt-3">
                <div class="col-12 text-center">
                    <a href="{{ path_for('index') }}" class="btn btn-link">
                        <i class="fas fa-home mr-2"></i> Повратак на почетну страну
                    </a>
                </div>
            </div>
        </div>
        <div class="d-none d-sm-none d-md-none d-lg-block d-xl-block mt-5">
            <div class="row mt-3">
                <div class="col-12 text-center">
                    <div class="btn-group">
                        <a href="{{ path_for('invoice', {orderId: order_number}) }}" class="btn btn-link">
                            <i class="fas fa-file-pdf mr-2"></i> Преузмите потврду о плаћању
                        </a>
                        <a href="{{ path_for('index') }}" class="btn btn-link">
                            <i class="fas fa-home mr-2"></i> Повратак на почетну страну ...
                        </a>
                    </div>

                </div>
            </div>
        </div>
    </div>
{% endblock %}